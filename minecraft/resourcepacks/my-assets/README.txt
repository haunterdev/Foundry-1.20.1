
Place your 16x16 PNG texture here:

-  resourcepacks/my-assets/assets/kubejs/textures/item/flux_woven_utherite.png

Notes:
- Use a 16x16 PNG named exactly: flux_woven_utherite.png
- This example uses the `kubejs` namespace so no mod .jar is required.
- If the item does not appear, restart the client/server and ensure the resource pack `my-assets` is enabled.
- To give the item in-game: `/give <yourname> kubejs:flux_woven_utherite`
